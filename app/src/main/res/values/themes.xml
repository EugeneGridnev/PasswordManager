<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PasswordManager" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PasswordManager" parent="Base.Theme.PasswordManager" />

    <style name="roundedImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">@dimen/card_corner_size</item>
    </style>

    <style name="cardTextViewStyle">
        <item name="android:layout_marginBottom">@dimen/card_text_view_style_margin</item>
        <item name="android:layout_marginTop">@dimen/card_text_view_style_margin</item>
        <item name="android:layout_marginStart">@dimen/card_text_view_style_margin</item>
        <item name="android:layout_marginEnd">@dimen/card_text_view_style_margin</item>
    </style>
</resources>